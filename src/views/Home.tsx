import { useAppSelector } from "@/redux/hooks";
import { RoleEnum } from "@/utils/enums";
import { Navigate } from "react-router-dom";

export default function Home() {
  return <div>This is Home Page</div>; // TODO: update UI later
}
